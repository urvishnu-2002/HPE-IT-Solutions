services:
  - type: web
    name: hpe-backend
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn your_application:wsgi
    envVars:
      - key: PYTHON_VERSION
        value: 3.14